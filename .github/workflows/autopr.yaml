name: Auto Pull Request

on:
  push:

jobs:
  autopr:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: autopr
      id: autopr
      uses: peter-evans/create-pull-request@v4
      with:
        base: dev
        token: ${{ github.token }}
        assignees: Dev0psPleb

